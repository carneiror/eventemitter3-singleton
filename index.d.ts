// Import the EventEmitter3 types
import { EventEmitter } from "eventemitter3";

/**
 * A registry instance powered by EventEmitter3.
 * You can use this to subscribe to and emit events.
 */
declare const registry: EventEmitter;

export default registry;
