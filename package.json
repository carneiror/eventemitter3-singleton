{
  "name": "eventemitter3-singleton",
  "version": "2.0.0",
  "description": "Just a wrapper to EventRegistry so we can have it as a singleton anywhere we import it.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -d ./index.ts --outDir ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eventemitter3": "*"
  },
  "peerDependencies": {
    "eventemitter3": "*"
  },
  "dependencies": {
    "typescript": "^5.8.2"
  }
}
